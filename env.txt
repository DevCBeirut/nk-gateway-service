export JWT_SECRET=mydirtylittlesecret

export REDIS_HOST=localhost
export REDIS_PORT=6379

export BACKEND_HOST=localhost
export BACKEND_PORT=1338

export REQUEST_MAX_ATTEMPTS=3

docker run -d --name employee-system -v /mnt/employeeSystem:/var/lib/arangodb3 --restart always --memory="1200m" --cpus="1.5" -p 8529:8529 -e ARANGO_STORAGE_ENGINE=rocksdb -e ARANGO_ROOT_PASSWORD=openSesame arangodb/arangodb:3.6.3
docker run -d --name redis -v /mnt/redis:/data --restart always -p 6379:6379 redis:6.0.5 redis-server --appendonly yes